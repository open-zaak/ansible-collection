apiVersion: v1
kind: Secret
data:
  tls.crt: "{{lookup('file', '{{ openzaak_tls_secrets.crt }}')|b64encode }}"
  tls.key: "{{lookup('file', '{{ openzaak_tls_secrets.key }}')|b64encode }}"
type: kubernetes.io/tls
